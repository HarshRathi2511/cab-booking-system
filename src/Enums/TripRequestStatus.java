package Enums;

public enum TripRequestStatus {
    //only 2 options for the user to respond with  (if not both of them , no_response )
    ACCEPTED,
    REJECTED,
    NO_RESPONSE, //no reponse when a feedback recd 
}
